# Crates

Rust workspace implementation boundaries. The runnable crates are protocol, storage, runtime,
network, intelligence, node, and cli; their dependency direction is documented in the root README.
